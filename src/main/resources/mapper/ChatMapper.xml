<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chat.repository.ChatDAO">

    <!-- 메시지 저장 -->
    <insert id="insertMessage">
        INSERT INTO chat_message(
            room_id,
            sender,
            message
        )
        VALUES(
            #{roomId},
            #{sender},
            #{message}
        )
    </insert>

    <!-- 메시지 조회 -->
    <select id="getMessages" resultType="chat.model.ChatMessageVO">
        SELECT *
        FROM chat_message
        WHERE room_id = #{roomId}
        ORDER BY send_time
    </select>

    <!-- 채팅방 생성 -->
    <insert id="createRoom" useGeneratedKeys="true" keyProperty="roomId">
    INSERT INTO chat_room(
        username1,
        username2
    )
    VALUES(
        #{username1},
        #{username2}
    )
</insert>

    <!-- 채팅방 조회 -->
    <select id="getRoom" resultType="chat.model.ChatRoomVO">
        SELECT *
        FROM chat_room
        WHERE room_id = #{roomId}
    </select>

    <!-- 채팅방 찾기 -->
    <select id="findRoom" resultType="long">
    SELECT room_id
    FROM chat_room
    WHERE (username1 = #{userName1}
       AND username2 = #{userName2})
       OR
          (username1 = #{userName2}
       AND username2 = #{userName1})
</select>

</mapper>